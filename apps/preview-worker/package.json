{
  "name": "@k-source-atlas/preview-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist --env=\"\"",
    "deploy": "wrangler deploy --env production",
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "wrangler types --check && tsc -p tsconfig.json"
  },
  "dependencies": {
    "@k-source-atlas/catalog": "workspace:*",
    "fast-xml-parser": "^5.3.0",
    "hono": "^4.12.30",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "miniflare": "4.20260710.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.111.0"
  }
}
